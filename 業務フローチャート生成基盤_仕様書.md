# 業務フローチャート生成基盤 仕様書

**対象構成:** JSON正本 + React Flow + ELK
**目的:** 業務文書・規程・手順書等から抽出した業務プロセスを、編集可能かつ自動レイアウト可能なフローチャートとして可視化する

---

## 1. 背景と目的

### 1.1 背景

業務フローチャートをAIで生成したい場合、画像生成AIに直接図を描かせる方法には以下の課題がある。

* ノードやエッジの接続関係が不安定
* 条件分岐や差戻しの表現が曖昧になりやすい
* 複雑な業務では1枚に収まらず、複数枚構成が必要
* 修正・差分管理・レビューが難しい
* 根拠資料との対応関係を保持しにくい
* 将来的な再利用や別ビュー生成に弱い

一方、業務フローの本質は画像ではなく、**業務上の意味構造**である。
そのため、AIには図そのものではなく、ノード・エッジ・レーン・条件分岐・根拠資料などの**意味モデル**を生成させ、描画は既存のレイアウトエンジンに委ねる方針を採用する。

### 1.2 目的

本仕様の目的は以下である。

* 業務プロセスをJSONの意味モデルとして表現する
* React Flow + ELK により、自動レイアウトされた可視化UIを実現する
* ノード重なり、エッジ交差、複雑な分岐などの描画課題を自前実装せず既存エンジンに委譲する
* 将来的な監査用途拡張を見据え、根拠資料やレビュー状態を保持できる構造にする
* 1つの正本JSONから複数のビューや将来的な複数ページ出力に拡張可能な基盤とする

---

## 2. 設計思想

### 2.1 基本方針

本システムでは、業務フローを以下の3層に分離する。

#### A. 意味モデル層

業務上の意味や構造を表現する層。
JSON正本はこの層のみを持つ。

例:

* どの処理が存在するか
* 誰が担当するか
* どのフェーズに属するか
* どの順序・条件で遷移するか
* どの資料が根拠か

#### B. レイアウト層

ノード配置やエッジ経路を決定する層。
ELK が担当する。

例:

* ノードのx/y座標
* どの順序で並べるか
* エッジの曲がり方
* ノードの重なり回避

#### C. 表示/UI層

ユーザーに見せるための描画・操作層。
React Flow が担当する。

例:

* ノード表示
* エッジ表示
* 選択・クリック
* 詳細パネル
* 将来的な手動微調整

### 2.2 責務分離

AIエージェント、JSON、ELK、React Flow の責務は以下の通り。

| 要素         | 責務                               |
| ---------- | -------------------------------- |
| AIエージェント   | 文書から意味構造を抽出しJSONを生成する            |
| JSON正本     | 業務フローの意味構造を保持する                  |
| ELK        | JSONからレイアウト可能なグラフを受け取り、自動座標計算を行う |
| React Flow | ELK結果を画面に表示し、操作可能なUIを提供する        |

### 2.3 重要原則

#### 原則1: JSONに座標を持たせない

JSON正本には、以下を含めない。

* x座標 / y座標
* ノード幅 / 高さ
* エッジの折れ線座標
* フォントサイズ
* 色の詳細
* ページ上の絶対位置

#### 原則2: 条件分岐はノードではなくエッジに持たせる

判断点は `decision` ノード、判断結果は `edge.condition` として表現する。

#### 原則3: 正本は画像ではなく意味モデル

描画結果は派生物であり、唯一の正本はJSONとする。

#### 原則4: 将来拡張を阻害しない

初期仕様は `lanes / phases / nodes / edges / sources` を中心としつつ、将来的にレビュー状態、信頼度、サブフロー、複数ページ化などへ拡張できる構造とする。

---

## 3. システム全体構成

### 3.1 処理フロー

全体フローは以下とする。

```text
業務文書 / 規程 / 手順書 / 既存フロー図
        ↓
AIエージェント
        ↓
flow_model.json（意味モデル）
        ↓
バリデーション
        ↓
ELK用グラフへ変換
        ↓
ELKレイアウト計算
        ↓
React Flowノード/エッジへ変換
        ↓
画面表示
```

### 3.2 入出力

#### 入力

* AIエージェントが生成した `flow_model.json`
* 必要に応じてUI設定ファイル

#### 出力

* React Flow上の可視化
* 将来的にはSVG/PNG/PDF出力も可能

---

## 4. JSON正本のスコープ

### 4.1 今回使用する主要要素

今回の仕様では、以下を正本JSONの中核とする。

* `lanes`
* `phases`
* `nodes`
* `edges`
* `sources`

### 4.2 今回は含めないもの

初期実装では以下は必須対象外とする。

* risks
* controls
* evidences
* subprocesses
* pages
* view_hints

ただし、将来拡張可能な前提で設計する。

---

## 5. JSONスキーマ仕様

## 5.1 ルート構造

```json
{
  "schema_version": "1.0",
  "flow_id": "string",
  "title": "string",
  "description": "string",
  "lanes": [],
  "phases": [],
  "nodes": [],
  "edges": [],
  "sources": []
}
```

### 各項目の意味

| 項目             | 必須 | 説明             |
| -------------- | -- | -------------- |
| schema_version | 必須 | JSONスキーマのバージョン |
| flow_id        | 必須 | フローの一意識別子      |
| title          | 必須 | フロー名           |
| description    | 任意 | フローの説明         |
| lanes          | 必須 | レーン定義          |
| phases         | 必須 | フェーズ定義         |
| nodes          | 必須 | ノード定義          |
| edges          | 必須 | エッジ定義          |
| sources        | 任意 | 根拠資料定義         |

---

## 5.2 lanes

### 目的

レーンは、担当部門・役割・システムなど、ノードの所属先を表す。

### 構造

```json
{
  "id": "L001",
  "name": "申請部門",
  "type": "department"
}
```

### 項目

| 項目   | 必須 | 説明    |
| ---- | -- | ----- |
| id   | 必須 | レーンID |
| name | 必須 | レーン名  |
| type | 必須 | レーン種別 |

### typeの例

* `department`
* `role`
* `system`
* `external_party`

---

## 5.3 phases

### 目的

フェーズは、業務の流れを意味的に区切る単位を表す。

### 構造

```json
{
  "id": "PH001",
  "name": "申請"
}
```

### 項目

| 項目   | 必須 | 説明     |
| ---- | -- | ------ |
| id   | 必須 | フェーズID |
| name | 必須 | フェーズ名  |

---

## 5.4 nodes

### 目的

ノードは、業務フロー上の処理、判断、開始、終了などの単位を表す。

### 構造

```json
{
  "id": "N001",
  "type": "process",
  "label": "購買申請を起票する",
  "lane_id": "L001",
  "phase_id": "PH001",
  "description": "申請部門が購買申請をシステムに登録する",
  "source_refs": ["SRC001"]
}
```

### 項目

| 項目          | 必須 | 説明       |
| ----------- | -- | -------- |
| id          | 必須 | ノードID    |
| type        | 必須 | ノード種別    |
| label       | 必須 | 表示ラベル    |
| lane_id     | 必須 | 所属レーンID  |
| phase_id    | 必須 | 所属フェーズID |
| description | 任意 | 詳細説明     |
| source_refs | 任意 | 根拠資料ID配列 |

### node.typeの例

* `start`
* `end`
* `process`
* `decision`
* `document`
* `system_process`

### 方針

* `label` は短く表示向けにする
* `description` は詳細パネル向けにする
* `decision` は分岐条件そのものではなく、判断点の説明を書く

---

## 5.5 edges

### 目的

エッジはノード間の遷移関係を表す。

### 構造

```json
{
  "id": "E001",
  "from": "N001",
  "to": "N002",
  "label": "承認",
  "edge_type": "normal",
  "condition": {
    "type": "text",
    "text": "承認条件を満たす"
  },
  "source_refs": ["SRC002"]
}
```

### 項目

| 項目          | 必須 | 説明       |
| ----------- | -- | -------- |
| id          | 必須 | エッジID    |
| from        | 必須 | 開始ノードID  |
| to          | 必須 | 終了ノードID  |
| label       | 任意 | 表示ラベル    |
| edge_type   | 任意 | エッジ種別    |
| condition   | 任意 | 分岐条件     |
| source_refs | 任意 | 根拠資料ID配列 |

### edge_typeの例

* `normal`
* `rollback`
* `exception`
* `escalation`

---

## 5.6 condition

### 目的

条件分岐を表す。
判断点は `decision` ノード、分岐条件は `edge.condition` に持たせる。

### 基本構造

```json
{
  "type": "text",
  "text": "承認条件を満たす"
}
```

### 初期実装で採用する形式

初期実装では `type: "text"` を標準とする。

```json
{
  "type": "text",
  "text": "金額が100万円以上"
}
```

### 将来拡張可能な形式

```json
{
  "type": "threshold",
  "field": "amount",
  "operator": ">=",
  "value": 1000000,
  "unit": "JPY",
  "text": "金額が100万円以上"
}
```

```json
{
  "type": "and",
  "conditions": [
    {
      "type": "text",
      "text": "金額が100万円以上"
    },
    {
      "type": "text",
      "text": "重要購買である"
    }
  ],
  "text": "100万円以上かつ重要購買"
}
```

### 初期方針

* UI表示とAI出力安定性を優先し、まずは `text` 中心
* 厳密な条件式評価エンジンは初期実装の対象外

---

## 5.7 sources

### 目的

各ノード・エッジの根拠資料を表す。

### 構造

```json
{
  "id": "SRC001",
  "file_name": "購買規程.pdf",
  "page": 3,
  "section": "購買申請"
}
```

### 項目

| 項目        | 必須 | 説明     |
| --------- | -- | ------ |
| id        | 必須 | ソースID  |
| file_name | 必須 | ファイル名  |
| page      | 任意 | ページ番号  |
| section   | 任意 | 節タイトル  |
| quote     | 任意 | 該当記述抜粋 |

---

## 6. JSON記述ルール

### 6.1 IDルール

* `lanes`: `L001`, `L002` ...
* `phases`: `PH001`, `PH002` ...
* `nodes`: `N001`, `N002` ...
* `edges`: `E001`, `E002` ...
* `sources`: `SRC001`, `SRC002` ...

### 6.2 参照整合性

* `node.lane_id` は必ず `lanes.id` のいずれかを参照する
* `node.phase_id` は必ず `phases.id` のいずれかを参照する
* `edge.from`, `edge.to` は必ず `nodes.id` のいずれかを参照する
* `source_refs` は必ず `sources.id` のいずれかを参照する

### 6.3 条件分岐表現ルール

* 判断点は必ず `type: "decision"` ノードで表す
* 分岐条件は `edge.condition` で表す
* `decision` ノードから出る複数のエッジは、原則として `label` を持つ
* 差戻しは `edge_type: "rollback"` を推奨する

### 6.4 表示ラベルルール

* `node.label` は原則40文字以内を推奨
* 長文は `description` に入れる
* `edge.label` は短く、詳細は `condition.text` に入れる

---

## 7. React Flow + ELK の採用理由と役割

## 7.1 React Flow 採用理由

React Flow は、ノード・エッジ型の可視化UIに適している。

### 利点

* ノード・エッジの表示が容易
* カスタムノードを作りやすい
* 詳細パネルとの連携が容易
* 将来的にドラッグ編集や折りたたみを実装しやすい
* Reactアプリとの親和性が高い

## 7.2 ELK 採用理由

ELK は、グラフの自動レイアウトに強い。

### 利点

* ノードの重なり回避
* エッジの自然な経路計算
* 複雑な分岐・差戻しにも比較的強い
* 大規模グラフにも対応しやすい
* レーンやグルーピングを考慮したレイアウトに拡張しやすい

## 7.3 役割分担

| コンポーネント    | 役割                        |
| ---------- | ------------------------- |
| React Flow | 描画と操作                     |
| ELK        | 座標計算とレイアウト                |
| JSON正本     | 意味構造                      |
| 変換ロジック     | JSON→ELK、ELK→React Flow変換 |

---

## 8. 実装構成

### 8.1 推奨モジュール構成

```text
src/
  domain/
    flow-model.ts
    validators.ts
  parsers/
    json-schema.ts
  layout/
    build-elk-graph.ts
    run-elk-layout.ts
    map-elk-to-reactflow.ts
  ui/
    FlowCanvas.tsx
    nodes/
      ProcessNode.tsx
      DecisionNode.tsx
      StartNode.tsx
      EndNode.tsx
    panels/
      NodeDetailPanel.tsx
      SourcePanel.tsx
```

### 8.2 処理の責務

#### flow-model.ts

JSONモデルの型定義

#### validators.ts

整合性チェック

#### build-elk-graph.ts

JSON正本からELK用グラフ構造へ変換

#### run-elk-layout.ts

ELKにレイアウト計算を依頼

#### map-elk-to-reactflow.ts

ELK結果をReact Flowのノード・エッジ形式へ変換

#### FlowCanvas.tsx

React Flow を使って画面表示

---

## 9. 型定義の考え方

TypeScriptでは以下のような型を持つ。

```ts
type Lane = {
  id: string;
  name: string;
  type: "department" | "role" | "system" | "external_party";
};

type Phase = {
  id: string;
  name: string;
};

type NodeType = "start" | "end" | "process" | "decision" | "document" | "system_process";

type Condition =
  | {
      type: "text";
      text: string;
    }
  | {
      type: "threshold";
      field: string;
      operator: ">=" | ">" | "<=" | "<" | "==" | "!=";
      value: number | string;
      unit?: string;
      text: string;
    }
  | {
      type: "and" | "or";
      conditions: Condition[];
      text: string;
    };

type FlowNode = {
  id: string;
  type: NodeType;
  label: string;
  lane_id: string;
  phase_id: string;
  description?: string;
  source_refs?: string[];
};

type FlowEdge = {
  id: string;
  from: string;
  to: string;
  label?: string;
  edge_type?: "normal" | "rollback" | "exception" | "escalation";
  condition?: Condition;
  source_refs?: string[];
};

type SourceRef = {
  id: string;
  file_name: string;
  page?: number;
  section?: string;
  quote?: string;
};

type FlowModel = {
  schema_version: string;
  flow_id: string;
  title: string;
  description?: string;
  lanes: Lane[];
  phases: Phase[];
  nodes: FlowNode[];
  edges: FlowEdge[];
  sources?: SourceRef[];
};
```

---

## 10. バリデーション仕様

## 10.1 必須チェック

* `schema_version` が存在すること
* `flow_id`, `title` が存在すること
* `lanes`, `phases`, `nodes`, `edges` が配列であること

## 10.2 ID重複チェック

* 各配列内で `id` が重複しないこと

## 10.3 参照整合性チェック

* `node.lane_id` が存在する `lane.id` を参照していること
* `node.phase_id` が存在する `phase.id` を参照していること
* `edge.from`, `edge.to` が存在する `node.id` を参照していること
* `source_refs` が存在する `source.id` を参照していること

## 10.4 構造チェック

* `start` ノードが最低1つ存在すること
* `end` ノードが最低1つ存在することを推奨
* `decision` ノードから出るエッジが2本以上あることを推奨
* 孤立ノードが存在しないことを推奨

## 10.5 表示品質チェック

* `label` が極端に長すぎないこと
* `decision` ノードの出力エッジに `label` が無い場合は警告
* 同一 `decision` ノードから出るエッジの `label` 重複は警告

---

## 11. レイアウト仕様

## 11.1 レイアウト方針

初期実装では以下を基本とする。

* 主方向: 左から右
* レーン: 上下に並べる
* フェーズ: 左から右のグルーピング要素として使う
* 差戻し: 逆方向エッジとして表現される場合がある

## 11.2 JSONに持たせないもの

* 各ノード位置
* ノード間距離
* ノードサイズ
* エッジの折れ線点

## 11.3 ELKへ渡す際に決めるもの

* ノードの仮の幅・高さ
* 全体の向き
* ノード間スペース
* レーンやフェーズのグルーピング方法

### 注意

これらは正本JSONではなく、レイアウト変換層で決定する。

---

## 12. React Flow 表示仕様

## 12.1 ノード表示

ノードタイプごとに表示を分ける。

| type           | 表示              |
| -------------- | --------------- |
| start          | 開始ノード           |
| end            | 終了ノード           |
| process        | 通常処理ノード         |
| decision       | ひし形相当または判断専用ノード |
| document       | 文書生成・参照ノード      |
| system_process | システム処理ノード       |

## 12.2 エッジ表示

* `label` を表示可能にする
* `edge_type` に応じてスタイル変更可能にする

  * `normal`: 通常線
  * `rollback`: 差戻し強調
  * `exception`: 例外ルート強調

## 12.3 詳細表示

ノード選択時に詳細パネルを表示する。

表示項目:

* label
* description
* lane名
* phase名
* source_refs の一覧

エッジ選択時に詳細パネルを表示してもよい。

表示項目:

* label
* condition.text
* edge_type
* source_refs

---

## 13. 条件分岐の設計方針

### 13.1 基本方針

* 判断点はノード
* 判断結果はエッジ
* 条件はエッジに持つ

### 13.2 理由

この分離により、以下が可能になる。

* 同じ判断点から複数分岐を自然に表現できる
* React Flow + ELK のノード/エッジモデルに一致する
* 差戻しや例外ルートを素直に表現できる
* 将来的に条件ごとの強調表示や分析が可能

### 13.3 例

```json
{
  "nodes": [
    {
      "id": "N003",
      "type": "decision",
      "label": "承認条件を満たすか",
      "lane_id": "L002",
      "phase_id": "PH002"
    }
  ],
  "edges": [
    {
      "id": "E003",
      "from": "N003",
      "to": "N004",
      "label": "承認",
      "edge_type": "normal",
      "condition": {
        "type": "text",
        "text": "承認条件を満たす"
      }
    },
    {
      "id": "E004",
      "from": "N003",
      "to": "N005",
      "label": "差戻し",
      "edge_type": "rollback",
      "condition": {
        "type": "text",
        "text": "承認条件を満たさない"
      }
    }
  ]
}
```

---

## 14. 今回の初期実装範囲

### 14.1 対象

* JSON正本の型定義
* バリデーション
* React Flow表示
* ELKによる自動レイアウト
* `lanes / phases / nodes / edges / sources` 対応
* `decision + edge.condition` による条件分岐対応
* ノード/エッジの詳細表示

### 14.2 対象外

* 複数ページ自動分割
* サブフロー折りたたみ
* 手動レイアウト保存
* BPMN互換出力
* 条件式の実行評価
* 監査リスク/統制/証跡の詳細モデル
* 複数ビュー切替

---

## 15. 将来拡張方針

### 15.1 監査メタ情報追加

将来的に以下を追加できるようにする。

* risks
* controls
* evidences
* confidence
* review_status

### 15.2 ビュー分離

同じ正本JSONから以下の表示が可能になる設計とする。

* 通常フロー
* 条件分岐強調ビュー
* ソース資料強調ビュー
* 例外ルート強調ビュー

### 15.3 複数ページ化

将来的に `pages` を追加し、長いフローを複数ページへ分割できるようにする。

---

## 16. 実装上の推奨事項

### 16.1 正本と描画結果を分離する

レイアウト済みの `position` を正本JSONへ逆保存しないことを推奨する。

### 16.2 AI生成JSONをそのまま描画しない

必ずバリデーションと正規化を通す。

### 16.3 まずは条件を `text` 中心にする

初期段階から複雑な論理式評価を入れすぎない。

### 16.4 描画スタイルはJSONではなくUI側で管理する

色、線種、フォントはテーマ層で管理する。

---

## 17. まとめ

本仕様の中核は以下である。

* 正本は画像ではなくJSONの意味モデル
* JSONには業務上の構造のみを保持する
* 座標や描画情報は持たせない
* 条件分岐は `decision` ノードと `edge.condition` で表す
* React Flow は描画/UI、ELK は自動レイアウトを担当する
* これにより、重なり解消や配線最適化を自前で実装せずに済む
* 将来的な監査用途拡張や複数ビュー化にも耐えられる

---
